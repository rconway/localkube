volumeClaim:
  name: eoepca-userman-pvc
  create: false
config:
  domain: test.172.17.0.1.nip.io
  volumeClaim:
    name: eoepca-userman-pvc
opendj:
  # This can be useful to workaround helm 'failed to upgrade' errors due to
  # immutable fields in the 'um-login-service-persistence-init-ss' job
  # persistence:
  #   enabled: false
  volumeClaim:
    name: eoepca-userman-pvc
oxauth:
  volumeClaim:
    name: eoepca-userman-pvc
oxtrust:
  volumeClaim:
    name: eoepca-userman-pvc
global:
  domain: test.172.17.0.1.nip.io
  nginxIp: 172.17.0.1
nginx:
  ingress:
    # annotations:
    #   cert-manager.io/cluster-issuer: letsencrypt-staging
    # hosts:
    #   - test.172.17.0.1.nip.io
    # tls:
    #   - hosts:
    #       - test.172.17.0.1.nip.io
    #     secretName: login-service-tls
    enabled: true
    annotations: {}
      # kubernetes.io/ingress.class: nginx
      # kubernetes.io/tls-acme: "true"
    path: /
    hosts:
      - test.172.17.0.1.nip.io
    tls: 
    - secretName: tls-certificate
      hosts:
        - test.172.17.0.1.nip.io
